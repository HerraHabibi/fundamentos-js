// --- VARIABLE CONST ---

  const usuario = 'Eric';
  console.log(usuario);

// Las variables const no se pueden reasignar
  const profesion = 'Bombero';
  //profesion = 'Médico';     // -> Esto da error
  console.log(profesion);

// Las variables let se pueden iniciar sin un valor
  //const material;           // -> Esto da error
  //console.log(material);